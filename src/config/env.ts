// const host="25.0.71.30:8080"
const host = "localhost:8080"

export const BACKEND_URL: string = "http://" + host + "/API/" //import.meta.env.VITE_BACKEND_URL;
export const BACKEND_WEBSOCKET_URL: string = "ws://" + host + "/API/ws"
